// TODO
//

import { removeElement } from "../../js/220728";

describe("", () => {
  test("", () => {
    expect(removeElement([3, 2, 2, 3], 3)).toBe(2);
  });
  test("", () => {
    expect(removeElement([0, 1, 2, 2, 3, 0, 4, 2], 2)).toBe(5);
  });
});
